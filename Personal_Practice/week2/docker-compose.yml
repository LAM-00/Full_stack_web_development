version: "3.9"
services:
  web:
    build: .
    container_name: week2-node
    ports:
      - "3000:3000"
  
# NO NEED for volumes
#    stdin_open: true    <--only for shells
#    tty: true       <-- not for servers


